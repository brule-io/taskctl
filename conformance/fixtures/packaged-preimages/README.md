# Explicit packaged-parity inputs

These two fictional native import images were emitted by the canonical JVM
conformance tests at the exact source and successful build recorded in
`origin.json`. Their byte digests are checked before packaged parity starts.
They contain independently authored object-work and complex-planning specimens,
not consumer source or fabricated native closure receipts.

Both shipped implementations receive these identical historical inputs. Keeping
them as source fixtures allows a clean release-smoke checkout to exercise all
process cases without Java, Gradle or leftovers in `build/proof`. Dynamic native
import generation remains exercised separately by the JVM/native test corpus.

Preserve these bytes and provenance. A future fixture version needs a distinct
reviewed identity; it must not silently rewrite this historical input. Process
comparison performs no protocol-specific normalization to hide differences.
