{"protocol":"tasking/core-draft-2","id":"TASK.protocol.value-boundary","title":"Keep typed values stable across mutable collection aliases","state":"open","intent":"Repair the reproduced constructor-to-plan validity breach before native-v1 assessment, while preserving valid protocol identities and exact opaque data.","requires":["TASK.migration.daemon-descendants","TASK.migration.dropzone"],"requirements":["ObjectValue and ArrayValue must own immutable backing collections so caller aliases cannot invalidate a constructed typed value, including nested payloads and keys.","Shared mutating boundaries must reject invalid programmatic record inputs before producing a writable initialization plan or persisting ledger state; include post-construction changes to borrowed record collections.","Preserve valid canonical hashes, numeric category/scale, Unicode, source provenance and historical evidence. Use typed code without Any, unchecked casts or broad Native Image allowances. No cross-repository authority or implicit operations."],"acceptance":["Regression cases reproduce mutable alias failures and prove stable values, rejected invalid plans/writes, valid cold reconstruction and unchanged unrelated bytes/mtimes.","Shared JVM/native conformance and packaged process parity pass; old valid hash/evidence witnesses remain unchanged. Record the actual validity boundary and any remaining limits."],"required_extensions":[],"extensions":{},"verification":["conformance","parity","review"]}
