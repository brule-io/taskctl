# Native planning encoding witnesses

These public planning records were read from the committed taskctl producer ledger
by the unchanged native executable at `3e504e6`, before audited planning was
implemented. `sources.json` binds the full producer source commit, locally verified
native distribution digest, input Git/ledger revisions and exact JSON hashes.
The executable bytes were compared to the digest-verified archive before capture.
The observations preserve native `tasking/planning-draft-1` encoding; they do not
invent earlier source-dialect history or assert native draft2 provenance.
