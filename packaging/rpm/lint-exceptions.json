{
  "invalid-license": {
    "pattern": "taskctl\\.(?:x86_64|src): W: invalid-license LicenseRef-taskctl-license-pending",
    "reason": "The owner has not selected a project license. This explicit pending tag grants none; this is a private upstream package, not a Fedora inclusion candidate."
  },
  "invalid-url": {
    "pattern": "taskctl\\.spec: W: invalid-url Source1: taskctl-rpm-support\\.tar\\.gz",
    "reason": "Source1 is locally generated packaging support, not a remote download. Its exact checksum and bytes are included in the self-contained source RPM."
  },
  "potential-bashisms": {
    "pattern": "taskctl\\.x86_64: W: potential-bashisms /usr/share/taskctl/bootstrap/taskctl",
    "reason": "checkbashisms mistakes calls to the wrapper's own POSIX hash() function for the Bash hash builtin. Preserve the canonical template; its SHA adapter and offline execution are covered by the existing wrapper corpus and Fedora test."
  },
  "non-coherent-filename": {
    "pattern": "taskctl\\.(src|x86_64): W: non-coherent-filename taskctl-(\\d+\\.\\d+\\.\\d+)-([a-z]+\\.\\d+)-(\\d+\\.fc44)\\.\\1\\.rpm taskctl-\\2~\\3-\\4\\.\\1\\.rpm",
    "reason": "GitHub rewrites tilde characters in asset names. Only the exact tilde-to-hyphen transport spelling is allowed; RPM header EVR and DNF upgrade ordering remain unchanged."
  }
}
